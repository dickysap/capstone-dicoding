import 'package:flutter/material.dart';

class DaftarBerobat extends StatelessWidget {
  static const routeName = '/daftar-berobat';
  const DaftarBerobat({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
